#include <iostream>
using namespace std;

int main()
{

    // array declaration
    // int nums[5];

    // array declaration and initialization
    int nums[] = {1, 2, 3, 4, 5};

    // standard for execution for an array
    for (int i = 0; i < 5; i++)
    {
        cout << nums[i] << endl;
    }
}